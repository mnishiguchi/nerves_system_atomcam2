# 2026-08-06 引き継ぎ書(AtomCam2 Nerves / RTSP・画像色・ダッシュボード)

2026-08-05 の引き継ぎ書(RTSP停止・IR-cut極性・暗部の紫)を引き継ぎ、
同日中にさらに 2 件の不具合調査(RTSP/ダッシュボード競合、ISP キャリ
ブレーション欠落)とダッシュボード UI 改善を行った。本書は
2026-08-06 終了時点の現状・未了・注意点をまとめたもの。

---

## 0. 実機・現状サマリ

| 項目 | 状態 |
| --- | --- |
| 対象機 | **192.168.1.32**(開発用カナリヤ機、[[atomcam2-canary-device]])/ **192.168.1.77** |
| 稼働ファーム | **camd VERSION 35**(両機に投入済み・working tree に保持、**未コミット**) |
| RTSP | 動作(両機とも `phase: :running` 確認済み)。v4l2rtspserver の `:epipe` crash-loop は今日も churning のたびに再発したが**放置で毎回 settle**(既知パターン、恒久対策は無し) |
| 画像色 | **正常**。フル解像度(1920x1080)JPEG + ISP キャリブレーションファイル同梱で紫化なしを実機 2 台で確認済み |
| JPEG/H.264 競合 | **解消済み**(独立バッファ化、VERSION 30〜) |
| ダッシュボード | 1.32: 既定 ON。1.77: 検証都合で一時 OFF にしていたが後で ON に戻した。全操作ボタンが Ajax 化されページ遷移しない(§1-D) |
| ネットワーク | 1.32 の wlan0 に会社 SSID(`NERVES_WIFI_SSID_2`/`_PASSPHRASE_2`)をランタイムのみで追加・接続確認済み(**reboot で消える**。恒久化は未実施、ユーザ判断待ち) |

**未コミットの理由**: 今日 1 日で camd VERSION 29→35 まで進み、ダッシュ
ボードの UI も複数変更した。**コミット前にユーザ確認**を取ること
(このリポジトリの一貫した運用ルール)。

---

## 1. 今日判明した真因と修正

### (A) RTSP がダッシュボード ON で無出力 wedge する

- ダッシュボードの映像タブが `/snapshot.jpg` を毎秒ポーリング →
  camd の **JPEG エンコーダチャンネルが H.264 チャンネルとバッファを
  共有**していたため、並行アクセスで無出力 wedge。
- 実機 A/B(192.168.1.77、reboot 20+ 回)で
  **ダッシュボード無効化 20/20 成功 vs 有効化 3/3 失敗**を確認し確定。
- **修正(VERSION 30)**: JPEG チャンネルを独立バッファ化(当初 640x360
  に縮小)。
- 詳細: [RTSP と Web ダッシュボードの競合調査](20260806_RTSPとWebダッシュボード競合_技術相談.md)、
  [JPEG/H.264 チャンネル分離 提案書](20260806_JPEGH264チャンネル分離_提案書.md)。

### (B) 画面全体がマゼンタ/紫色になる(フル解像度化で顕在化)

- JPEG チャンネルをフル解像度(1920x1080)に上げたところ(VERSION 31)、
  実機 2 台で画面全体が紫化。当初「フル解像度化による ISP 資源圧迫」
  と誤診断し 640x360 に戻した(VERSION 32)が、**1 台(1.32)は戻しても
  紫が残った**。
- **決定打**: 1.32 の Nerves SD を抜きメーカー純正ファーム+純正アプリ
  で確認 → **自然な発色**。同一ハードウェアで発色が変わった = ソフト
  ウェア側の問題と確定(ハード故障ではない)。
- **真因**: `/etc/sensor/gc2053-t31.bin`(ISP キャリブレーションファイル)
  が Nerves rootfs に存在しなかった。`ISP: open ... failed` ログは以前
  から両機に出ていたが「両機とも同じだから無害」と誤認していた。
- **修正(VERSION 34)**: 実機の `/atom/system/etc/sensor/gc2053-t31.bin`
  (2 台で SHA256 完全一致、機種共通データ)を
  `package/atomcam2-camera/gc2053-t31.bin` としてリポジトリに追加し、
  `$(TARGET_DIR)/etc/sensor/` へインストール。
- **フル解像度は VERSION 35 で再度有効化し、両機とも紫化しないことを
  確認済み**。「フル解像度化 = 危険」ではなく「キャリブレーション欠落」
  が真因だったと実証。
- 診断用に `wb get|auto|daylight|cloudy|shade|manual <r> <b>` コマンド
  を camd.ctl に追加(VERSION 33)。対称な手動ゲイン探索は収束しない
  ことを確認済み(対症療法、本命はキャリブレーションファイル)。
- 詳細: [ISP キャリブレーション欠落 技術相談](20260806_ISPキャリブレーション欠落_技術相談.md)。

### (C) ダッシュボード UI 改善

- 映像タブ: アプリ/camd バージョン表示、リフレッシュ 0.5 秒化、
  スナップ表示トグル削除、夜間 ON/OFF/自動ボタンを復元。
- 操作タブ・動作確認タブ: 全ボタンをページ遷移なしの Ajax 化。
- 詳細: [ダッシュボード UI 改善 実装報告書](20260806_ダッシュボードUI改善_実装報告書.md)。

### (D) 前日(2026-08-05)からの引き継ぎ事項(解決済み)

- RTSP がすぐ止まる(night ISP stall / v4l2rtspserver epipe)→ 修正済み
  (camd VERSION 24)。
- 白レースカーテンが紫(IR-cut 極性逆転)→ 修正済み(VERSION 29)。
- 「暗部の紫」は当時「低照度 ISP AWB 由来」と推定していたが、**実際は
  上記(B)のキャリブレーション欠落が真因**だったと判明(訂正)。

---

## 2. コード・Git 状態

- **ブランチ**: `checkpoint/20260805-night-fix`(origin push 済み、前日
  時点)。今日の変更はこのブランチの working tree 上でさらに進んでいる。
- **未コミット差分(working tree、= camd VERSION 35)**:
  - `package/atomcam2-camera/camd.c`(JPEG/H.264 分離、フル解像度、
    `wb` 診断コマンド、`CAMD_VERSION`/`/tmp/camd.version`)
  - `package/atomcam2-camera/atomcam2-camera.mk`(VERSION 35、
    `gc2053-t31.bin` の INSTALL_TARGET_CMDS 追加)
  - `package/atomcam2-camera/atomcam2-camd`(ビルド済みバイナリ)
  - `package/atomcam2-camera/gc2053-t31.bin`(**新規**、ISP キャリブレー
    ションファイル、159,736 バイト)
  - `package/atomcam2-camera/README.md`
  - `examples/atomcam2_nerves_app/lib/atomcam2_nerves_app/camera_native.ex`
    (`camd_version/0`、`@snapshot_min_interval_ms` 500ms)
  - `examples/atomcam2_nerves_app/lib/atomcam2_nerves_app/dashboard/{view.ex,router.ex,collector.ex}`
    (Ajax 化、バージョン表示、夜間ボタン移設)
  - `docs/20260806_*.md`(本書含め 5 本)
- **コミット前にユーザ確認**(このリポジトリの一貫ルール)。

---

## 3. ビルド・投入手順

```bash
# camd(C, uClibc/1.1.1)クロスビルド
D=/tmp/claude-1000/-home-owner-piyopiyoex-nerves-system-atomcam2/39fe2451-5dc9-4559-9436-240a9efe8d5e/scratchpad
GCC=$D/mips-gcc472/bin/mips-linux-gnu-gcc
SDK_INC=$D/sdk111
SAMPLES_INC=$D/atomcam_tools/custompackages/package/ingenic_samples/libimp-samples
DEVLIBS=$D/ingenic-lib/T31/lib/1.1.1/uclibc/4.7.2
cd package/atomcam2-camera
"$GCC" -muclibc -msoft-float -O2 -march=mips32r2 \
  -I"$SDK_INC" -I"$SAMPLES_INC" \
  -Wl,--dynamic-linker=/atom/lib/ld-uClibc.so.0 \
  camd.c -L"$DEVLIBS" -limp -lalog -lpthread -lm -lrt \
  -o atomcam2-camd
# -muclibc -msoft-float が必要(このツールチェーンは multilib、
# 何も付けないと glibc/hard-float 版が選ばれヘッダが見つからない)。
# libimp.so 自体は hard-float ビルドなので "uses -mhard-float" の
# リンカ警告が出るが無害(既存バイナリでも同じ警告が出る)。

# ファーム(camd.c 変更時は .mk の VERSION を必ず +1。local package は
# ファイル変更だけでは再ビルドされず stale が乗る)
cd examples/atomcam2_nerves_app
export MIX_TARGET=atomcam2 ATOMCAM2_SYSTEM_SOURCE=local \
  ATOMCAM2_KERNEL_IMAGE=/home/owner/piyopiyoex/nerves_system_atomcam2/target/atomcam2-control/factory_t31_ZMC6tiIDQN
mise exec -- mix firmware
mise exec -- mix upload <ip>
# upload 後は Nerves.Runtime.reboot() で有効化(スロット切替は次回起動)
```

- カーネル SHA ピン(`b50658ea…`)は**絶対に変更しない**。
- `ATOMCAM2_KERNEL_IMAGE` は `target/atomcam2-control/factory_t31_ZMC6tiIDQN`
  を指す(リポジトリ内、SHA256 検証済み)。**未設定だとビルドが
  `ATOMCAM2_KERNEL_IMAGE must point to...` で失敗する**。
- ビルドコマンドは必ず `examples/atomcam2_nerves_app` に `cd` してから
  実行すること。バックグラウンド実行後は cwd がリセットされることが
  あるため、次のコマンドで `cd` を省略すると
  `Your project is using Nerves bootstrap but doesn't depend on Nerves`
  で失敗する。
- OTP は mise 経由(OTP29)。詳細は [運用リファレンス](20260805_リファレンス.md) §8。

---

## 4. デバイス取り扱いの鉄則(重要、前日から継続)

- **`killall -9` 連打・過剰 reboot をしない**。T31 ISP/VPU が wedge し、
  camd init 成功でも sensor/ISP 無出力になり得る。
- プロセス停止は必ず **SIGTERM**。
- **物理電源断は提案禁止**(ユーザ明言)。今日も 1.32 が完全無応答に
  なった際、ユーザが自主的に電源を入れ直した(こちらから提案しない)。
- **推測で断定しない**。今日も「フル解像度化が原因」「IR-cut 機械摩耗」
  と 2 度誤診断し、いずれもユーザの指摘(「ハード故障と決めつけるな」
  「フル解像度より後に症状が出た」)で軌道修正した。**1 手ごとに結果
  (キャプチャ/ログ)を見てから結論する**。
- **ハード/ソフトの切り分けに迷ったら、同じハードウェアで別のソフト
  スタックを動かして比較する**のが最も確実(今日はメーカー純正ファーム
  起動で証明した)。
- 応答は**日本語**で。
- カナリヤ機は 192.168.1.32([[atomcam2-canary-device]])。実験はここを
  優先し、他機は断りなく叩かない。

---

## 5. 画像キャプチャの確実な取得法

```elixir
# IEx(ssh <ip>)で
File.rm("/tmp/camd.jpg"); File.touch("/tmp/camd.snap")
Process.sleep(2500)
b = File.read!("/tmp/camd.jpg")
IO.puts(Base.encode64(b))
```
→ ローカルで `sed 's/^:ok//' raw.txt | base64 -d > out.jpg`
(SSH 出力が `:ok` と base64 が同一行に連結されるため `sed` で除去する。
改行区切りと誤認して `tail -n +2` 等で行を丸ごと捨てると全データを
失うので注意)。

- ダッシュボードの映像タブを開いたブラウザが起動中だと `/snapshot.jpg`
  ポーリングと競合し `/tmp/camd.jpg` が短時間で消えることがある
  (`File.read!` が enoent になる)。**2〜3 回リトライすれば通る**。
- **camd の snap 成功自体が camd 健全性の指標**(snap_ok = フレーム
  生成中)。

---

## 6. 未了タスク(新セッションはここから)

1. **今日の変更のコミット判断**: camd VERSION 35 + ダッシュボード UI
   改善一式。**コミット前にユーザ確認**。
2. **1.32 の wlan0 会社 SSID 恒久化**: 現状ランタイムのみ
   (`VintageNet.configure/2`)、reboot で消える。恒久化するなら
   `NERVES_WIFI_SSID_2`/`NERVES_WIFI_PASSPHRASE_2` を渡してビルド
   し直す([[atomcam2-wired-lan]] のマルチ拠点 Wi-Fi 方式)。
3. **v4l2rtspserver :epipe crash-loop の恒久対策**(保留・低優先、前日
   から継続): 現状は「叩かなければ settle」で運用回避。
4. **HardwareTest.ircut の極性/ラベル**(任意、前日から継続、未着手)。

---

## 7. 参考文書・メモリ

- 本日の技術文書:
  [RTSP/ダッシュボード競合](20260806_RTSPとWebダッシュボード競合_技術相談.md) /
  [JPEG/H.264 チャンネル分離 提案書](20260806_JPEGH264チャンネル分離_提案書.md) /
  [ISP キャリブレーション欠落](20260806_ISPキャリブレーション欠落_技術相談.md) /
  [ダッシュボード UI 改善](20260806_ダッシュボードUI改善_実装報告書.md)
- 前日文書: [運用リファレンス](20260805_リファレンス.md)、
  [作業記録](20260805_作業記録.md)
- メモリ(`~/.claude/.../memory/`): `atomcam2-rtsp-stability`,
  `atomcam2-device-handling`, `atomcam2-canary-device`,
  `atomcam2-dashboard-rtsp-conflict`, `atomcam2-isp-calibration`,
  `atomcam2-gpio-peripherals`, `atomcam2-rtsp-rtp-routing`,
  `atomcam2-build-toolchain`, `atomcam2-control-kernel`,
  `github-ops-token`
- GitHub 操作は curl + PAT(gh CLI 不通)。repo `piyopiyoex/nerves_system_atomcam2`。
